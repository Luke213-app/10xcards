/**
 * Sandbox fixture used to exercise the AI Code Review pipeline (Phase 4 of the
 * `code-review` change). Pure, dependency-free helper — safe to delete.
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
